name: Test Email Step

on:
  push:
    branches:
      - main   # or the branch you want to trigger the workflow

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: 24f2000838@ds.study.iitm.ac.in
        run: echo "Hello, world!"
